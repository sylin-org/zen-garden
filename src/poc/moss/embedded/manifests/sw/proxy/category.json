{
  "$schema": "../category.schema.json",
  "name": "proxy",
  "description": "Reverse proxies, load balancers, and API gateways",
  "aliases": [
    "gateway",
    "load-balancer",
    "lb",
    "ingress",
    "reverse-proxy"
  ],
  "tags": [
    "proxy",
    "networking",
    "routing"
  ],
  "connection": {
    "protocol": "http",
    "uri_template": "http://{host}:{port}"
  }
}
