{
  "$schema": "../category.schema.json",
  "name": "timeseries",
  "description": "Time-series databases for metrics and IoT data",
  "aliases": ["tsdb", "time-series", "metrics-db", "iot"],
  "default_protocol": "http",
  "connection_template": "http://{host}:{port}",
  "tags": ["timeseries", "metrics", "iot"]
}
