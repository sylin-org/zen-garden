# The generated contract artifact must be byte-stable across platforms -
# the embedded copy is compared against regeneration in tests (R4.7);
# CRLF checkouts would break the equality.
src/v1/crates/contract/surface.json text eol=lf
