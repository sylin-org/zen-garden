{
  "$schema": "../category.schema.json",
  "name": "secrets",
  "description": "Secret management and credential storage",
  "aliases": [
    "vault",
    "credentials",
    "keys",
    "secret-management"
  ],
  "tags": [
    "secrets",
    "security",
    "credentials"
  ],
  "connection": {
    "protocol": "http",
    "uri_template": "http://{host}:{port}"
  }
}
