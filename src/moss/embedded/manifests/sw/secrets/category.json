{
  "$schema": "../category.schema.json",
  "name": "secrets",
  "description": "Secret management and credential storage",
  "aliases": ["vault", "credentials", "keys", "secret-management"],
  "default_protocol": "http",
  "connection_template": "http://{host}:{port}",
  "tags": ["secrets", "security", "credentials"]
}
